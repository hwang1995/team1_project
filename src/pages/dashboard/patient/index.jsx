import React from 'react';
/**
 * 이 페이지 컴포넌트는 환자 관리 페이지를 작성하기 위한 컴포넌트입니다.
 * 들어가야할 내용은 다음과 같습니다.
 * * Sider
 * * Header
 * * 환자 관리 (PatientSearch, Table, ColoredButton)
 * @returns {JSX.Element}
 */
const PatientPage = () => {
  return (
    <div>
      PatientPage를 작성합니다. 들어가야 힐 컴포넌트는 위의 주석에 설명되어
      있으니 참조하시면 됩니다.
    </div>
  );
};

export default PatientPage;
